
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.newphone.app',
  appName: 'New Phone',
  webDir: 'dist',
  server: {
    url: 'https://msrsr177-sketch.github.io/new-phone1/',
    cleartext: true
  }
};

export default config;
